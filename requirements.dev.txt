# Include the production stack so development mirrors deployment
-r requirements.txt

# Test runners and coverage reporting
pytest==8.4.2  # Primary Python test runner
pytest-django==4.11.1  # Django-specific pytest helpers
pytest-asyncio==0.23.7  # Asyncio support for pytest used by Channels tests
pytest-cov==7.0.0  # pytest plugin integrating coverage
coverage==7.10.6  # Coverage metrics for test suites

# Static analysis and formatting
ruff==0.13.1  # Fast linting and formatting assistance

# Tooling dependencies pinned for deterministic local environments
exceptiongroup==1.3.0  # Improved tracebacks for Python test runs
iniconfig==2.1.0  # Configuration parser consumed by pytest
packaging==25.0  # Version parsing utilities used by tooling
platformdirs==4.4.0  # Cross-platform cache directory helpers for tooling
pluggy==1.6.0  # Plugin system underpinning pytest
Pygments==2.19.2  # Rich tracebacks and syntax highlighting
tomli==2.2.1  # TOML parser for tooling configuration
tomlkit==0.13.3  # Advanced TOML manipulation for Ruff
